# About Tom Tran

## Who I am

Tom Tran is a Software Engineer.

## My tools

Day to day Tom uses:

- Gmail (imhttan@gmail.com)
- Google Drive and Google Calendar
- Zed
- Java Spring

## How I like to work

Tom values clarity over cleverness. He wants trade-offs surfaced explicitly on architecture decisions, not a single recommendation handed to him. He prefers plain English, no jargon, direct communication. He will push back if something is being overcomplicated. He expects to be challenged if there is a better approach.

For research tasks, start searching immediately. Don't gate on clarifying questions.

Never delete, send, or publish anything without checking with him first. Flag it when he's overcomplicating something or going down a rabbit hole.

## Workspace structure

- `~/codework/outputs/` — all output files go here. Each project gets its own subfolder, e.g. `~/codework/outputs/project-name/`.
- `~/codework/projects/` — per-project context files (memory, notes, CLAUDE.md). Each project gets a matching subfolder, e.g. `~/codework/projects/project-name/`.

If it's unclear whether something is a new project, ask. Otherwise, create matching subfolders in both `outputs/` and `projects/` for new work.
